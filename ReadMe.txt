Only Code.sql file can be run in database.

CREATE cout_source.sql, CREATE TABLE.sql, FOREIGN_KEY_FINAL.sql, PRIMARY_KEY_FINAL,sql all cannot be run, because all the actions have been done in our database.
They are for displaying what we have done during our establishment of our database.